{
  "name": "frontend",
  "version": "1.0.0",
  "description": "My swell Nuxt.js project",
  "author": "Watit Thammarat",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "s3": "aws s3 cp dist s3://www.dekdurian.com/ --recursive",
    "deploy": "npm run build && npm run s3"
  },
  "dependencies": {
    "nuxt": "^1.0.0"
  },
  "devDependencies": {
    "cross-env": "^5.0.1"
  }
}
